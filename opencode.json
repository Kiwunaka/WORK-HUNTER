{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "work-hunter": {
      "type": "local",
      "command": ["python", "-m", "work_hunter", "--root", ".", "mcp"],
      "enabled": true,
      "timeout": 10000
    }
  }
}
